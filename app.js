const c=require("./index");

c.average(3,5);
c.percentage(3,5);

//The value we are using may or may not need to be same as that of the const variable used to in index.js (this is for require module (1))
